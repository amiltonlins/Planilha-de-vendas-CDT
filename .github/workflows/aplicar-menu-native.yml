name: Aplicar navegação nativa

on:
  push:
    branches: [main]
    paths:
      - scripts/patch_menu_native.py
      - .github/workflows/aplicar-menu-native.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Aplicar correção
        run: python scripts/patch_menu_native.py
      - name: Validar sintaxe
        run: python -m py_compile app.py
      - name: Commitar app atualizado
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.py
          git diff --cached --quiet || git commit -m "Usar navegação nativa na mesma página [skip ci]"
          git push origin HEAD:main
